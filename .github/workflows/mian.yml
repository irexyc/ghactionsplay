name: ceshi
on: 
  #push:
  workflow_dispatch:
    inputs:
      dasd:
        description: 'a very very very very very very very very very very very very very very very very very very very very very very very very very very long nonsense'
        required: false
        default: '[ "a", "b" ]'
jobs:
  ceshix:
    name: ceshi
    runs-on: "windows-latest"
    steps:
      - name: ceshi
        shell: powershell
        run: |
          which pwsh
          which powershell
          docker run --name ceshi --rm mcr.microsoft.com/windows/servercore:ltsc2022 echo hello
